This folder contains client-side security scaffolding for the GodsEye frontend.

- `constraints.ts` defines non-negotiable security assumptions that UI code must respect.
- `threatModel.md` sketches the expected adversaries and design principles.

No cryptographic primitives are implemented here; see the `crypto/` folder for typed placeholders.


